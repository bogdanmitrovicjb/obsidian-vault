### What is done from previous week:
- restructured tests into groups
- found [this](https://youtrack.jetbrains.com/articles/IJPL-A-1048/How-to-find-action-ID)and replaced some stuff with actions, WHO TO ASK for more info (should this be used if I'm testing UI actually? is it some behind the scenes stuff or is it a proper way to test if ui is working ok)
- done work on the gutter icons test
- went through the checklists and wrote a test plan for consoles
- 



### What is left to be done:
- unfortunately, to finish setting up linux ruby stuff
- minitest tests
![[Screenshot 2025-07-15 at 14.57.39.png]]
### Questions:






Test plan with progress and short descriptions of what each test covers:

1. Projects
    - Create a Gem project — create a new Gem project, specify a Ruby interpreter and check if Bundler init works
    - Create a Rails API project — create a new Rails API project, specify a Ruby interpreter and Rails version, download rails gem and check files (in progress: invoking rails-specific actions to check if they work)
    - Create an empty Ruby project — create a new empty Ruby project, specify a Ruby interpreter
    - Open an existing Ruby project — create a project, create another project and then open the first one, check if loaded correctly
    - Open a Rails project from VCS — clone a project from VCS and trust it (in progress: test if features that only work when the project is trusted actually work)
    - Open a Rails project from VCS without trusting
2. Files & Directories
    - New Ruby script — create a ruby script and check if it's created successfully, recognized as a ruby script and offers gutter icons for running ruby code (in progress: gutter icon)
    - Renaming and moving files — refactor actions, rename, remove, test gutter stuff if it still shows correctly after refactoring
    - Checking if the test directory is marked as such
    - Checking icons (will be merged with the above test)
3. Consoles (completion, command history, etc.)
    - Rails console
    - Pry console
    - IRB console
4. Debugger
    - Mouse-controlled Ruby debugger — install debugger, start and stop debugging from the toolbar, step over, step into, run to cursor, resume (in progress: run to cursor)
    - Keyboard-controlled Ruby debugger — install debugger, start and stop debugging from the toolbar, step over, step into, run to cursor, resume (in progress: run to cursor), same as above but with shortcuts instead of clicking on ui elements
5. Tests
    - Minitest
    - RSpec
    - Cucumber
    - Other actions (jump to source, rerun test, etc.)
6. Rails navigation
	1. controller gutter icons
		1. controller to view
		2. view to controller
		3. model to scheme
		4. scheme to model
	2. go to routes from controller
	3. endpoints
	4. finding route through right click menu
	5. 