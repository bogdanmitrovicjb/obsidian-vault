### What I do for testing consoles
- Pry
	- run "gem install pry"
	- run pry
	- same as irb
	- make a Hello class with x=1 inside
	- use cd Hello
	- use ls -i
	- check output should be x
	- stop pry
	- stop the ide
- Irb
	- run irb
	- write "put"
	- check if "puts" is recommended
	- select "puts"
	- add ("abc")
	- run
	- go up once and check if "puts("abc")" is there
	- check if output is "abc"
	- write the following code and run
	- check if output is [2, 4, 6]
	- stop irb
	- stop the ide
```ruby
[1, 2, 3].map do |n|
  n * 2
end
```
- Rails
	- open sample rails app
	- run bundle install
	- run rake db:migrate
	- run rake db:seed
	- run rails server
	- start rails console
	- select default environment and run the console (development is always default?)
	- run User.first
	- type User. and check autocompletions (should it show all and first? it only shows new)
	- run User.all
	- go up 2 times
	- check if User.first is showing in the terminal
	- run app.root_path (not showing in autocomplete)
	- run app.get _ (also not showing)
	- run helper.time_ago_in_words 30.days.ago
	- check if output is "about 1 month"
	- stop the console
	- stop the rails server
	- stop the ide

### What I should do for testing consoles
- all
	- navigate through results/suggestions using arrows
	- several commands up in history
	- also check going down in history
- Pry
	- 
- Irb
	- 
- Rails
	- run all modes
	- skip seed stuff
	- skip running the rails server, not needed
	- tests for every environment
		- user.first
		- output
		- highlighting
		- completion popup
	- don't check for specific completion items

- actual Rails plan then
	- open sample rails app
	- run bundle install
	- run rake db:migrate
	- start rails console
	- select default environment and run the console (development is always default?)
	- run User.first
	- type User. and check autocompletions (should it show all and first? it only shows new)
	- run User.all
	- go up 2 times
	- check if User.first is showing in the terminal
	- run app.root_path (not showing in autocomplete)
	- run app.get _ (also not showing)
	- run helper.time_ago_in_words 30.days.ago
	- check if output is "about 1 month"
	- stop the console
	- stop the ide




![[Pasted image 20250724160438.png]]
![[Pasted image 20250724160452.png]



only after I finish all this which is started, and have some nice done tests pushed which pass green, only then can I add new stuff


EXPERIMENTAL TEST only if I have time
next week Tuesday build take a look, I have my tests and checklist for public builds, take this build, take some tests and try to check if this build is ok for publishing, basically manual qa, start with what I covered in my tests