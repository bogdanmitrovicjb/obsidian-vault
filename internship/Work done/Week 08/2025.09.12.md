- Started Cucumber tutorial
- Hi, I hope you can hear me well, I'm having some troubles with allergies AND wifi so not a good combination. Ok so I got the feedback that I should be more verbose at the daily standup as my progress may actually be interesting to you guys and that you may want to hear more details about my progress. Let me make a quick recapitulation for you, still I will try to be short. I will skip the whole process of learning about stuff, reading documentation and guides and trying things out manually and get to the good part immediately. So the initial test plan Karina and I made consists of many tests split into three big categories and many smaller sub-categories. We got Should be covered, Good to have and Less important & hard to make. From should be covered, which are tests that have the highest priority and need to be implemented, we have Project category (basically opening and closing projects, creating new projects of many different types and deleting them afterwards, this is mostly done except for a bug we encountered which causes New Ruby project items to be missing, basically all there is is an empty screen), then Rails subcategory (with tests such as rails server, rails console, port forwarding etc., there is progress here but I'm having some difficulties with ruby versions and ruby managers, still it's going pretty good), then there are Files and directories, test tree IRB and Pry console sub categories. From good to have there are bundler tests and ruby version detection because it's important but hard to make and is postponed for later. And from less important and hard to cover there is docker integration, typing tests (performance testing, not my task) and checking for correct interpreter which should be done by unit tests. That's about it, I will make sure to share my progress a bit more detailed from now on!



### Should be covered
- New project & open project
	- Trusted project (remove the trusted project after testing), easily reproduced with VCS
- Rails console, Rails server
	- port forwarding check
	- checks for completion
	- command history
- Files and directories, repetitive tasks but very easy to automate
	- new -> ruby script known bug
	- rename, move (files + dir)
	- if test dir is marked as such
- Test tree, different types of tests (minitest, rspec, cucumber)
	- tree can be broken in remdev, also RM bugs
	- start with checking if the tree looks good with minitest
- IRB console
	- checks for completion
	- command history
- Pry console
	- checks for completion
	- command history
### Good to have
- bundler
- Ruby version detection may be hard to achieve in auto UI tests due to no way of checking whether all version managers and versions are detected, all the info we have on these is from the IDE itself and we cannot assess if all are present (is that right? are there ways to check all installed version managers on a device from the test itself? does the build server even have any other ruby managers? when do auto UI tests run?)
### Less important or hard to cover
- docker integration, may introduce flakiness due to docker errors or misconfigured stuff
	- port forwarding check
- typing tests, not sure auto UI tests are a good choice since the delays are probably hard to track because of many keyboard inputs and lots of things to track (actual appearing of letters, suggestions, auto completion, highlighting etc.)
	- ruby extension for split mode/frontend, provides type assist, potentially add a check for this plugin to see whether it's listed
- check for correct interpreter, should be covered by unit tests